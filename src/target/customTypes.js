let drawPoint = (x, y, z, w, a, b) => {
    //...
};
// OBJECT
let drawPointObj = (point) => {
    //.. console x and y
};
drawPointObj({
    x: 1,
    y: 2
});
//IN LINE ANNOTATION
let drawPointInLineAnnotation = (point) => {
    //...
};
drawPointInLineAnnotation({
    x: 1,
    y: 2
});
let drawPointInterface = (point) => {
    //...
};
drawPointInterface({
    x: 1,
    y: 2
});
//COESÃO:
class PointClass1s {
    x;
    y;
    draw() {
        //....
    }
    getDistance(another) {
        //...
    }
}
//# sourceMappingURL=customTypes.js.map